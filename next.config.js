/**
 * Configuracion principal de Next.js.
 *
 * Aqui se agregan opciones globales del framework cuando el proyecto
 * necesita personalizar compilacion, imagenes, rutas u otros ajustes.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
